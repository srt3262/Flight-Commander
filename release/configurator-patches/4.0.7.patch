diff --git a/CHANGELOG.md b/CHANGELOG.md
index 1b5d11e70bb7c0efb3d2d798418df0191b562282..c0418b8b14e047893e740c58aac50944e37fe806 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,5 +1,14 @@
 # Flight Commander 4.0.0
 
+## 4.0.7
+
+- Restore the verified 4.0.5 accelerometer and gyro high-rate attitude paths after the 4.0.6 mirrored-roll regression.
+- Remove the additional 4.0.6 board-frame buffers and all writes to them from the live IMU update loops.
+- Derive compass-orientation accelerometer and gyro vectors on demand from the latest calibrated raw samples before user board alignment.
+- Preserve the MICOAIR743 BMI088 `CW270_DEG` target alignment and avoid any HUD-only roll sign workaround.
+- Retain learned IST8310 orientation, manual CW0 compass alignment, field calibration, stationary heading fusion and moving-baseline RTK support.
+- Retire 4.0.6 from the managed online flasher while preserving the verified 3.0.7 recovery baseline.
+
 ## 4.0.6
 
 - Replace the MICOAIR743 onboard IST8310 fixed transform with a persistent learned sensor-to-board signed-axis transform.
diff --git a/js/flightCommander/firmwareCatalog.js b/js/flightCommander/firmwareCatalog.js
index f8b4656642dee57d7c9c374e1b191c0d2538f5b7..9fc5202bd351f5d56db2c2668d4ebc55ef6998e4 100644
--- a/js/flightCommander/firmwareCatalog.js
+++ b/js/flightCommander/firmwareCatalog.js
@@ -3,7 +3,7 @@
 export const FLIGHT_COMMANDER_FIRMWARE_RELEASES_URL =
   "https://api.github.com/repos/srt3262/Flight-Commander/releases?per_page=20";
 
-export const FLIGHT_COMMANDER_MINIMUM_SUPPORTED_FIRMWARE_VERSION = "4.0.6";
+export const FLIGHT_COMMANDER_MINIMUM_SUPPORTED_FIRMWARE_VERSION = "4.0.7";
 export const FLIGHT_COMMANDER_KNOWN_GOOD_FIRMWARE_VERSIONS = Object.freeze([
   "3.0.7",
 ]);
diff --git a/js/main/ntripClient.js b/js/main/ntripClient.js
index 467e5cd7eb05381b065cad92df04d2f57fb8ab55..9290f8e6af271890f2a5c7d487e98affa496ae05 100644
--- a/js/main/ntripClient.js
+++ b/js/main/ntripClient.js
@@ -42,7 +42,7 @@ function buildRequest(config, requestPath) {
     `GET ${requestPath} HTTP/1.1\r\n` +
     `Host: ${hostHeader}:${config.port}\r\n` +
     'Ntrip-Version: Ntrip/2.0\r\n' +
-    'User-Agent: NTRIP FlightCommander/4.0.6\r\n' +
+    'User-Agent: NTRIP FlightCommander/4.0.7\r\n' +
     'Accept: */*\r\n' +
     'Cache-Control: no-cache\r\n' +
     authorizationHeader(config) +
diff --git a/release/notes/v4.0.7-beta.md b/release/notes/v4.0.7-beta.md
new file mode 100644
index 0000000000000000000000000000000000000000..88720b75f2e9946eb6d42396115c1605a2a30f83
--- /dev/null
+++ b/release/notes/v4.0.7-beta.md
@@ -0,0 +1,40 @@
+# Flight Commander 4.0.7 Beta
+
+Flight Commander 4.0.7 is a coordinated Configurator and MICOAIR743 firmware hotfix for the mirrored-roll regression reported in 4.0.6.
+
+## IMU handedness correction
+
+Firmware 4.0.6 added separate pre-board-alignment accelerometer and gyro buffers inside the live IMU update loops for learned compass orientation. On the Aero Selfie H743, the reported attitude could then become left/right mirrored: physically raising the right side displayed the left side rising on the HUD.
+
+4.0.7 removes every added buffer write from both high-rate IMU paths and restores the verified 4.0.5 processing sequence:
+
+1. Apply the fixed BMI088 sensor alignment.
+2. Apply the saved board alignment.
+3. Publish the attitude vector used by the estimator and telemetry.
+
+The compass-orientation learner still receives calibrated pre-board-alignment vectors, but now derives them on demand from the latest raw samples only when the 40 Hz compass task requests them. The target remains `IMU_BMI088_ALIGN CW270_DEG`; no HUD-only sign inversion or user alignment workaround is used.
+
+## Retained functionality
+
+The persistent learned IST8310 sensor-to-board transform, independent manual CW0 compass alignment, conventional compass field calibration, stationary heading availability, startup yaw initialization and moving-baseline RTK support remain enabled.
+
+The managed online flasher now offers the verified 3.0.7 recovery baseline and firmware 4.0.7 or later. Firmware 4.0.6 is excluded because of the IMU handedness regression.
+
+## Propeller-off acceptance check
+
+After flashing, keep the propellers removed and recalibrate the accelerometer. Verify all three attitude directions before arming:
+
+- Raise the controller's right side: the HUD right side must rise.
+- Raise the nose: the HUD nose must rise.
+- Rotate clockwise when viewed from above: yaw and heading must move in the corresponding direction.
+
+Then complete learned compass orientation and the conventional compass field calibration. This beta requires physical target-hardware acceptance before flight.
+
+## Deliverables
+
+The complete `Flight-Commander-v4.0.7.zip` contains exactly four files:
+
+1. `FC-Windows-v4.0.7.zip`
+2. `FC-Configurator-Source-v4.0.7.zip`
+3. `FC-Firmware-v4.0.7-MICOAIR743.hex`
+4. `FC-Firmware-Source-v4.0.7.zip`
diff --git a/tabs/landing.html b/tabs/landing.html
index bbb9676036d3532178ffe4fa0e8627b91eff3568..4d0e710117056c8d3df57a41d2a0ee4f14e04776 100644
--- a/tabs/landing.html
+++ b/tabs/landing.html
@@ -34,31 +34,30 @@
       </div>
       <div class="column half text2">
         <div class="wrap">
-          <h2>Flight Commander 4.0.6</h2>
+          <h2>Flight Commander 4.0.7</h2>
           <p>
-            Flight Commander 4.0.6 Beta introduces a learned onboard-compass
-            orientation workflow for the MICOAIR743. The firmware no longer
-            assumes a fixed signed-axis transform for the IST8310.
+            Flight Commander 4.0.7 Beta restores the MICOAIR743 IMU attitude
+            path after the 4.0.6 roll-handedness regression. Raising the physical
+            right side now remains a right-side-up roll throughout firmware and
+            HUD telemetry instead of being mirrored.
           </p>
           <p>
-            Complete the six-position accelerometer calibration first, then use
-            the guided six-face compass-orientation procedure. Rotate the entire
-            controller around the requested upward face until each position is
-            complete. Firmware compares synchronized accelerometer, gyro and
-            canonical magnetometer vectors across all 24 physically valid
-            right-handed axis mappings, rejects ambiguous data, and permanently
-            stores the winning transform with its confidence and residual.
+            The accelerometer and gyro once again use the verified 4.0.5
+            high-rate processing sequence. Compass-orientation learning derives
+            its synchronized board-frame samples only when the 40 Hz compass task
+            requests them, so learning no longer adds a second vector write inside
+            either attitude update loop.
           </p>
           <p>
-            The learned sensor transform is independent from manual compass
-            alignment and board alignment. Changing the learned transform clears
-            the previous magnetic offsets and gains, after which the normal
-            compass field calibration must be completed. Heading fusion excludes
-            the onboard compass until both stages are valid.
+            The persistent learned IST8310 transform, independent manual CW0
+            alignment, stationary heading availability and moving-baseline RTK
+            features from 4.0.6 are retained. Recalibrate the accelerometer, then
+            complete compass orientation and conventional compass calibration
+            after flashing.
           </p>
           <p>
             The managed flasher offers the verified 3.0.7 recovery baseline plus
-            4.0.6 and later. Failed 3.0.6-and-earlier, 3.0.8 and 4.0.0-through-4.0.5
+            4.0.7 and later. Failed 3.0.6-and-earlier, 3.0.8 and 4.0.0-through-4.0.6
             firmware remain excluded from the managed list.
           </p>
           <h2 style="margin-top: 1em">Open-source foundations</h2>
diff --git a/tests/flight-commander/firmware/flight-commander-catalog.test.mjs b/tests/flight-commander/firmware/flight-commander-catalog.test.mjs
index 16aa2423f5fcda2c188c7a4f5b56e93719375fd3..f43ee4cdd568f9a2d39d256b2085efa4c75bd473 100644
--- a/tests/flight-commander/firmware/flight-commander-catalog.test.mjs
+++ b/tests/flight-commander/firmware/flight-commander-catalog.test.mjs
@@ -33,7 +33,7 @@ describe("Flight Commander firmware catalog", () => {
       FLIGHT_COMMANDER_FIRMWARE_RELEASES_URL,
       "https://api.github.com/repos/srt3262/Flight-Commander/releases?per_page=20",
     );
-    assert.equal(FLIGHT_COMMANDER_MINIMUM_SUPPORTED_FIRMWARE_VERSION, "4.0.6");
+    assert.equal(FLIGHT_COMMANDER_MINIMUM_SUPPORTED_FIRMWARE_VERSION, "4.0.7");
     assert.deepEqual(FLIGHT_COMMANDER_KNOWN_GOOD_FIRMWARE_VERSIONS, ["3.0.7"]);
     assert.equal(isSupportedFlightCommanderFirmwareVersion("3.0.6"), false);
     assert.equal(isSupportedFlightCommanderFirmwareVersion("3.0.7"), true);
@@ -42,7 +42,8 @@ describe("Flight Commander firmware catalog", () => {
     assert.equal(isSupportedFlightCommanderFirmwareVersion("4.0.0"), false);
     assert.equal(isSupportedFlightCommanderFirmwareVersion("4.0.4"), false);
     assert.equal(isSupportedFlightCommanderFirmwareVersion("4.0.5"), false);
-    assert.equal(isSupportedFlightCommanderFirmwareVersion("4.0.6"), true);
+    assert.equal(isSupportedFlightCommanderFirmwareVersion("4.0.6"), false);
+    assert.equal(isSupportedFlightCommanderFirmwareVersion("4.0.7"), true);
     assert.equal(isSupportedFlightCommanderFirmwareVersion("4.1.0"), true);
     assert.equal(normalizeFirmwareTarget("MICOAIR743"), "MICOAIR743");
     assert.equal(normalizeFirmwareTarget("MICROAIR743"), "MICOAIR743");
@@ -92,19 +93,19 @@ describe("Flight Commander firmware catalog", () => {
       {
         draft: false,
         prerelease: true,
-        tag_name: "v4.0.6",
-        name: "Firmware 4.0.6 beta",
+        tag_name: "v4.0.7",
+        name: "Firmware 4.0.7 beta",
         html_url: "https://example.invalid/release",
         published_at: "2026-08-02T12:00:00Z",
         body: "Prop-off bench baseline.",
         assets: [
           {
-            name: "Flight-Commander-Firmware-4.0.6-MICOAIR743-BENCH-ONLY.hex",
+            name: "Flight-Commander-Firmware-4.0.7-MICOAIR743-BENCH-ONLY.hex",
             browser_download_url: "https://example.invalid/firmware.hex",
             digest: `sha256:${"a".repeat(64)}`,
             size: 1234,
           },
-          { name: "Flight-Commander-Firmware-4.0.6-source.zip" },
+          { name: "Flight-Commander-Firmware-4.0.7-source.zip" },
         ],
       },
     ];
@@ -118,12 +119,12 @@ describe("Flight Commander firmware catalog", () => {
   });
 
   test("uses standalone GitHub HEX assets for the current release and verified recovery baseline", () => {
-    const filename = "Flight-Commander-Firmware-4.0.6-MICOAIR743.hex";
+    const filename = "Flight-Commander-Firmware-4.0.7-MICOAIR743.hex";
     const online = flightCommanderReleaseDescriptors([
       {
         draft: false,
         prerelease: false,
-        tag_name: "v4.0.6",
+        tag_name: "v4.0.7",
         assets: [
           {
             name: "Flight-Commander-Firmware-3.0.7-MICOAIR743.hex",
@@ -142,7 +143,7 @@ describe("Flight Commander firmware catalog", () => {
     ]);
 
     assert.equal(online.length, 2);
-    assert.deepEqual(online.map(({ version }) => version), ["4.0.6", "3.0.7"]);
+    assert.deepEqual(online.map(({ version }) => version), ["4.0.7", "3.0.7"]);
     assert.equal(online[0].url, "https://example.invalid/firmware.hex");
     assert.equal(online[0].bytes, 1234);
     assert.equal(online[1].url, "https://example.invalid/recovery.hex");
@@ -231,7 +232,7 @@ describe("Flight Commander firmware catalog", () => {
 });
 
 
-test("keeps 3.0.7 and 4.0.6 while removing every verified-bad intervening release", () => {
+test("keeps 3.0.7 and 4.0.7 while removing every verified-bad intervening release", () => {
   const releaseFor = (version, marker) => ({
     draft: false,
     prerelease: true,
@@ -255,6 +256,7 @@ test("keeps 3.0.7 and 4.0.6 while removing every verified-bad intervening releas
     releaseFor("4.0.4", "4"),
     releaseFor("4.0.5", "5"),
     releaseFor("4.0.6", "a"),
+    releaseFor("4.0.7", "b"),
   ]);
-  assert.deepEqual(descriptors.map(({ version }) => version), ["4.0.6", "3.0.7"]);
+  assert.deepEqual(descriptors.map(({ version }) => version), ["4.0.7", "3.0.7"]);
 });
diff --git a/tests/flight-commander/firmware/imu-handedness-regression.test.mjs b/tests/flight-commander/firmware/imu-handedness-regression.test.mjs
new file mode 100644
index 0000000000000000000000000000000000000000..5d8fd2f65912480d59d427fad715ddddbb46fd37
--- /dev/null
+++ b/tests/flight-commander/firmware/imu-handedness-regression.test.mjs
@@ -0,0 +1,62 @@
+import assert from 'node:assert/strict';
+import { mkdtempSync, readFileSync, rmSync } from 'node:fs';
+import { tmpdir } from 'node:os';
+import { dirname, join, resolve } from 'node:path';
+import { spawnSync } from 'node:child_process';
+import { after, test } from 'node:test';
+import { fileURLToPath } from 'node:url';
+
+const projectRoot = resolve(dirname(fileURLToPath(import.meta.url)), '../../..');
+const packageManifest = JSON.parse(readFileSync(join(projectRoot, 'package.json'), 'utf8'));
+const firmwareVersion = packageManifest.flightCommander.firmwareSourceVersion;
+const firmwareSourceArchive = packageManifest.flightCommander.firmwareSourceArchive;
+const output = mkdtempSync(join(tmpdir(), `flight-commander-${firmwareVersion}-imu-handedness-`));
+const python = process.env.PYTHON || (process.platform === 'win32' ? 'python' : 'python3');
+const archive = join(projectRoot, ...firmwareSourceArchive.split('/'));
+const result = spawnSync(
+  python,
+  ['-c', 'import sys,zipfile; zipfile.ZipFile(sys.argv[1]).extractall(sys.argv[2])', archive, output],
+  { encoding: 'utf8' },
+);
+assert.equal(result.status, 0, `${firmwareVersion} source extraction failed:\n${result.stdout}\n${result.stderr}`);
+
+const sourceRoot = join(output, `Flight-Commander-Firmware-Source-v${firmwareVersion}`);
+const source = (relative) => readFileSync(join(sourceRoot, ...relative.split('/')), 'utf8');
+const targetHeader = source('src/main/target/MICOAIR743/target.h');
+const acceleration = source('src/main/sensors/acceleration.c');
+const gyro = source('src/main/sensors/gyro.c');
+const orientation = source('src/main/flight_commander/compass_orientation.c');
+
+after(() => rmSync(output, { recursive: true, force: true }));
+
+test('MICOAIR743 retains its fixed BMI088 rotation instead of reflecting one axis', () => {
+  assert.match(targetHeader, /#define IMU_BMI088_ALIGN\s+CW270_DEG/);
+  assert.doesNotMatch(targetHeader, /IMU_BMI088_ALIGN\s+CW270_DEG_FLIP/);
+});
+
+test('accelerometer attitude path has no 4.0.6 side buffer or mirrored write', () => {
+  assert.doesNotMatch(acceleration, /accBoardFrameG/);
+  assert.match(
+    acceleration,
+    /applySensorAlignment\(accADC, accADC, acc\.dev\.accAlign\);\s*applyBoardAlignment\(accADC\);/,
+  );
+  assert.match(acceleration, /arm_sub_f32\(acc\.dev\.ADCRaw, fAccZero, zeroed, XYZ_AXIS_COUNT\)/);
+  assert.match(acceleration, /applySensorAlignment\(calibrated, calibrated, acc\.dev\.accAlign\)/);
+  assert.match(acceleration, /arm_scale_f32\(calibrated, 1\.0F \/ acc\.dev\.acc_1G, result/);
+});
+
+test('gyro attitude path has no 4.0.6 side buffer or mirrored write', () => {
+  assert.doesNotMatch(gyro, /gyroBoardFrameDps/);
+  assert.match(
+    gyro,
+    /applySensorAlignment\(gyroADCtmp, gyroADCtmp, gyroDev->gyroAlign\);\s*applyBoardAlignment\(gyroADCtmp\);\s*\/\/ Convert to deg\/s and store in unified data\s*arm_scale_f32\(gyroADCtmp, gyroDev->scale, gyroADCf, 3\);/,
+  );
+  assert.match(gyro, /arm_sub_f32\(gyroDev\[0\]\.gyroADCRaw, gyroDev\[0\]\.gyroZero, calibrated/);
+  assert.match(gyro, /applySensorAlignment\(calibrated, calibrated, gyroDev\[0\]\.gyroAlign\)/);
+  assert.match(gyro, /arm_scale_f32\(calibrated, gyroDev\[0\]\.scale, result/);
+});
+
+test('compass learning still consumes the on-demand pre-board IMU vectors', () => {
+  assert.match(orientation, /accGetBoardFrame\(normalizedAcc\)/);
+  assert.match(orientation, /gyroGetBoardFrame\(gyroVector\)/);
+});
diff --git a/tests/flight-commander/packaging/package-contract.test.mjs b/tests/flight-commander/packaging/package-contract.test.mjs
index 8ec750c5572b15ab561d74ea3250839ac9c8e7a5..ad70a376e4e1f0dec40de4c6dde60e11961037e1 100644
--- a/tests/flight-commander/packaging/package-contract.test.mjs
+++ b/tests/flight-commander/packaging/package-contract.test.mjs
@@ -581,13 +581,13 @@ test("application remains dark-only", () => {
 });
 
 test("firmware is release-only and the flasher exposes local, online, then flash", () => {
-  assert.equal(packageManifest.flightCommander.firmwareReleaseVersion, "4.0.6");
+  assert.equal(packageManifest.flightCommander.firmwareReleaseVersion, "4.0.7");
   assert.equal(packageManifest.flightCommander.firmwareChangedInRelease, true);
   assert.equal(packageManifest.flightCommander.firmwareSourceAvailable, true);
-  assert.equal(packageManifest.flightCommander.firmwareSourceVersion, "4.0.6");
+  assert.equal(packageManifest.flightCommander.firmwareSourceVersion, "4.0.7");
   assert.equal(
     packageManifest.flightCommander.firmwareSourceArchive,
-    "release/firmware/Flight-Commander-Firmware-Source-v4.0.6.zip",
+    "release/firmware/Flight-Commander-Firmware-Source-v4.0.7.zip",
   );
   const releaseFirmwareIsPresent = existsSync(firmwareReleasePath);
   const releaseSourceIsPresent = existsSync(firmwareSourcePath);
@@ -690,7 +690,7 @@ test("all requested large-prop INAV presets are wired into the release source",
 });
 
 test("landing page reports the current Flight Commander release", () => {
-  assert.equal(packageManifest.version, "4.0.6");
+  assert.equal(packageManifest.version, "4.0.7");
   assert.equal(manifest.version, packageManifest.version);
   assert.match(
     landingHtml,
