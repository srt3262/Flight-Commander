/*
 * Flight Commander Firmware additions are licensed under GNU GPL v3.
 *
 * This program is free software: you can redistribute it and/or modify
 * it under the terms of the GNU General Public License as published by
 * the Free Software Foundation, either version 3 of the License, or
 * (at your option) any later version.
 */

#pragma once

#include <stdint.h>

#define FLIGHT_COMMANDER_VERSION_MAJOR 4
#define FLIGHT_COMMANDER_VERSION_MINOR 0
#define FLIGHT_COMMANDER_VERSION_PATCH 8

#define FLIGHT_COMMANDER_STR_HELPER(value) #value
#define FLIGHT_COMMANDER_STR(value) FLIGHT_COMMANDER_STR_HELPER(value)
#define FLIGHT_COMMANDER_VERSION_STRING \
    FLIGHT_COMMANDER_STR(FLIGHT_COMMANDER_VERSION_MAJOR) "." \
    FLIGHT_COMMANDER_STR(FLIGHT_COMMANDER_VERSION_MINOR) "." \
    FLIGHT_COMMANDER_STR(FLIGHT_COMMANDER_VERSION_PATCH)

#define FLIGHT_COMMANDER_INFO_SIGNATURE "FCFW"
#define FLIGHT_COMMANDER_INFO_SIGNATURE_LENGTH 4
#define FLIGHT_COMMANDER_INFO_SCHEMA_VERSION 1
#define FLIGHT_COMMANDER_INFO_PAYLOAD_SIZE 15

typedef enum {
    FLIGHT_COMMANDER_CAPABILITY_MULTIROTOR_AUTOTUNE = (1U << 0),
    FLIGHT_COMMANDER_CAPABILITY_TERRAIN_WAYPOINTS = (1U << 1),
    FLIGHT_COMMANDER_CAPABILITY_MISSION_STREAMING = (1U << 2),
    FLIGHT_COMMANDER_CAPABILITY_RTK_GPS_UART = (1U << 3),
    FLIGHT_COMMANDER_CAPABILITY_DRONECAN = (1U << 4),
    FLIGHT_COMMANDER_CAPABILITY_DRONECAN_GPS = (1U << 5),
    FLIGHT_COMMANDER_CAPABILITY_NATIVE_GCS_COMMANDS = (1U << 6),
    FLIGHT_COMMANDER_CAPABILITY_PHOTO_TRIGGERS = (1U << 7),
    FLIGHT_COMMANDER_CAPABILITY_DRONECAN_NODE_CONFIG = (1U << 8),
    FLIGHT_COMMANDER_CAPABILITY_MISSION_RESUME = (1U << 9),
    FLIGHT_COMMANDER_CAPABILITY_GCS_RTK_BASE = (1U << 10),
    FLIGHT_COMMANDER_CAPABILITY_HEADING_FUSION = (1U << 11),
    FLIGHT_COMMANDER_CAPABILITY_MOVING_BASELINE_YAW = (1U << 12),
    FLIGHT_COMMANDER_CAPABILITY_DRONECAN_MOVING_BASELINE_MANAGER = (1U << 13),
    FLIGHT_COMMANDER_CAPABILITY_COMPASS_ORIENTATION_LEARNING = (1U << 14),
    FLIGHT_COMMANDER_CAPABILITY_INDIVIDUAL_COMPASS_CALIBRATION = (1U << 15),
} flightCommanderCapability_e;

#define FLIGHT_COMMANDER_CAPABILITIES ((uint32_t)0xFFFFU)
