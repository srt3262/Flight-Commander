Flight Commander 4.0.6 beta release trigger.

This is the only active release trigger. The matching workflow builds and verifies the MICOAIR743 firmware, tests and packages the Windows Configurator, assembles exactly four canonical deliverables, uploads the complete bundle once, integrates the retained source, publishes v4.0.6-beta, and re-downloads the published ZIP for byte-for-byte verification.
